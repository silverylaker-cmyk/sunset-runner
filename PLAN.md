# SUNSET RUNNER — OutRun 스타일 모바일 레이싱 게임 계획

## 목표
- 1986년 OutRun 풍의 의사 3D(pseudo-3D) 레이싱 게임을 **빌드 없는 정적 웹앱**으로 제작
- 스마트폰 가로 모드 전용, 정적 호스팅(GitHub Pages / Vercel / Netlify)에 올려 HTTPS로 접속
- 조작: **폰을 핸들처럼 돌려서 조향**, 화면 좌/우 끝 **터치 페달**(왼쪽 브레이크, 오른쪽 액셀)

## 결정 사항
| 항목 | 결정 |
|---|---|
| 게임 모드 | 체크포인트 타임어택. 5개 스테이지, 체크포인트 통과 시 시간 보너스, 마지막 스테이지 끝이 GOAL |
| 테스트/배포 | 정적 호스팅. 기울기 센서는 HTTPS 필수 |
| 화면 방향 | 가로 전용. 세로면 "폰을 돌려주세요" 안내 |
| 기술 | 바닐라 JS(ES modules) + Canvas 2D, 외부 라이브러리 없음, 그래픽/사운드 전부 코드 생성 |

## 조작 설계
- **조향**: 센서로 중력 벡터를 구해 화면 평면에서 회전각 계산 → ±30°가 풀 조향. 데드존 2°, 저역 필터, 시작 시 자동 보정 + HUD 재보정 버튼
  - iOS 13+는 사용자 탭 안에서 `requestPermission()` 필요 → 시작 탭에서 센서 권한, 오디오 언락, 전체화면을 한 번에 처리
  - 화면 회전 방향(landscape-primary/secondary)에 따라 축·부호 자동 결정
  - 센서 미지원 시 화면 중앙 드래그로 조향, PC에서는 방향키
- **페달**: 좌우 끝 22% 영역, 포인터 이벤트 + pointerId 로 멀티터치, 동시 입력 허용
- **PC 테스트**: ←→ 조향, ↑/Space 액셀, ↓ 브레이크

## 파일 구성
```
index.html            캔버스 + 오버레이(타이틀/HUD/게임오버/회전안내) + 페달
style.css             풀스크린, touch-action:none, safe-area
js/main.js            DOM 연결, 시작 탭 처리, 리사이즈
js/game.js            고정 타임스텝 루프, 상태머신, 물리, 체크포인트/타이머
js/track.js           세그먼트 기반 트랙 생성(커브/언덕/스프라이트/교통 차량), 스테이지 테마
js/render.js          투영, 도로 폴리곤, 패럴랙스 배경, 절차적 스프라이트, 차량
js/input.js           센서 조향 + 터치 페달 + 키보드/드래그 폴백
js/audio.js           WebAudio 엔진음, 체크포인트/충돌 효과음
manifest.webmanifest  홈화면 추가 시 전체화면(iOS는 Fullscreen API 미지원)
icons/                앱 아이콘
README.md             배포/테스트 방법
```

## 게임 규칙
- 시작 75초, 체크포인트마다 +50초, 시간 0이면 TIME UP
- 최고 속도 300km/h 표기, 코너에서 원심력으로 바깥으로 밀림, 도로 밖은 감속 + 장애물 충돌
- 교통 차량 추돌 시 감속, 스테이지별 배경/도로/스프라이트 테마 변경
- 점수 = 주행 거리 + 남은 시간 보너스, 최고 기록 localStorage 저장

## 검증 계획
- PC Chrome에서 키보드로 렌더링/물리/HUD 확인
- 폰: 배포 후 실제 기기에서 센서 방향·감도 확인 (디버그 오버레이로 원시 센서값 표시)
